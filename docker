docker --version                                                     Said your docker version

docker build -t <your docker hub id>/<dll name> .                    Builds your project with docker

docker run -p 8080:80 -d <your docker hub id>/<dll name>             Executes your docker image

docker stop <container id>                                           Stops your instance

docker ps                                                            Lists your docker containers

docker start <container id>                                          Starts your container

docker push <your docker hub id>                                     Push your container to your docker online

docker-compose -f <YOUR YML FILE> up -d                              Execute/Download container image from file
